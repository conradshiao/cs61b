package enigma;

import java.util.Arrays;
import java.util.regex.Pattern;
import static enigma.PermutationData.rotorName;

public class practicetestmethods {
    public static void main(String[] unused) {
        /* String msg = "helloconradaustinjoellegracekevin";
        int count = 0;
        for (int i = 0; i < msg.length(); i++) {
            if ((count + 1) % 5 == 0) {
                System.out.print(msg.charAt(i) + " ");
            } else {
                System.out.print(msg.charAt(i));
            }
            count += 1;
        }
        String x = "ABC!";
        System.out.println(Pattern.matches("[A-Z]*", x));
        String[] z = {"hi", "bye", "conrad"};
        System.out.println(Arrays.asList(z));
        System.out.println(Arrays.asList(z)); */
        //String x = "ABCj";
        //System.out.println(Pattern.matches("[A-Z]*", x));
        String x = "AXLE";
    }
}

